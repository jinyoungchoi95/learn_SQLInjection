server:
  address: localhost
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password:
    url: jdbc:mysql://localhost:3306/sqlinjection?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
